---
title: "STM32F411 UART 실습 (Polling / Interrupt / DMA)"
categories:
  - STM32
  - UART
tags:
  - STM32F411
  - HAL
  - Interrupt
  - DMA
  - Polling
---

# 🧰 STM32F411 보드 UART 실습

> STM32CubeMX + HAL 기반 UART 실습  
> Polling, Interrupt, DMA 방식 비교

## 1️⃣ 개요
- **일시:** 2025.10.15 (수)
- **환경:** STM32F411RETx + CubeIDE
- **특징:** 비동기 UART 통신 구조 실습

## 2️⃣ UART 설정 코드

```c
void MX_USART2_UART_Init(void)
{
  huart2.Instance = USART2;
  huart2.Init.BaudRate = 115200;
  huart2.Init.WordLength = UART_WORDLENGTH_8B;
  huart2.Init.StopBits = UART_STOPBITS_1;
  huart2.Init.Parity = UART_PARITY_NONE;
  huart2.Init.Mode = UART_MODE_TX_RX;
  if (HAL_UART_Init(&huart2) != HAL_OK) Error_Handler();
}
```

## 3️⃣ Interrupt / DMA 구조
```c
void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
  HAL_UART_Receive_IT(&huart2, rxData, 4);
}
```

> RXNE 플래그 = 1 일 때 데이터 수신 준비 완료.

---

## 🧪 실험 결과 요약
| 구분 | 방식 | 결과 |
|------|------|------|
| RX | Interrupt | 정상 수신 |
| TX | Polling | 정상 송신 |
| DMA | 연속 데이터 수신 | 문제 없음 |

---

## 📎 참고자료
- [STM32F411 Datasheet](./docs/stm32f411ce.pdf)
- [MB1136 회로도](./docs/mb1136-default-c05_schematic.pdf)
